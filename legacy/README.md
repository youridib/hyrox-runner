# Legacy

`hyrox-runner-v2.html` is the original single-file app, kept for reference.

It is not built or deployed. Its planning logic now lives in `src/domain/`,
with the behaviour differences documented in the top-level README and covered
by tests.
